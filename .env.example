# Arquivo de exemplo de variáveis de ambiente.
# Resumo: modelo para configurar conexões (RabbitMQ, SMTP) e porta da API.

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672
RABBITMQ_QUEUE=notifications

# SMTP (dev: MailHog)
SMTP_HOST=mailhog
SMTP_PORT=1025

EMAIL_FROM=no-reply@example.com

# API
PORT=3000

# JWT / Auth (PoC)
JWT_SECRET=jwt_secret
JWT_EXPIRES_IN=1h
ADMIN_USER=admin
ADMIN_PASS=secret